{
  "name": "juriseasy",
  "version": "0.2.0",
  "private": true,
  "description": "JurisEasy — Digital notary for Philippine lawyers (compliant with A.M. No. 24-10-14-SC).",
  "scripts": {
    "build": "node scripts/build-config.js",
    "dev": "npx serve -l 3000 .",
    "config": "node scripts/build-config.js"
  },
  "engines": {
    "node": ">=18"
  }
}
